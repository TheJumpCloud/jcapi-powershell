{
    "type": "object",
    "properties": {
        "queryType": {
            "type": "string",
            "enum": ["Filter", "FilterQuery", "Search"]
        },
        "searchFilters": {
            "type": "string",
            "description": "For queryType 'Search', this is a stringified filter object."
        },
        "filters": {
            "type": "array",
            "items": {
            "type": "object",
            "properties": {
                "field": { "type": "string" },
                "operator": { "type": "string" },
                "value": { "type": "string" }
            },
            "required": ["field", "operator", "value"]
            },
            "description": "For queryType 'Filter' or 'FilterQuery', this is an array of filter objects."
        }
    },
    "description": "Query object for group membership."
}